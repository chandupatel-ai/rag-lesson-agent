# Pick ONE provider and fill its key
LLM_PROVIDER=anthropic          # or: openai
ANTHROPIC_API_KEY=your-key-here
OPENAI_API_KEY=your-key-here

# Optional: use a different (ideally stronger/different) model for the judge
GENERATOR_MODEL=
JUDGE_MODEL=
